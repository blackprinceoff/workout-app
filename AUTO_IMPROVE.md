# AUTO_IMPROVE — журнал автономного покращення

Цей файл — робоча пам'ять сесії `improver`: звідси починається кожна ітерація.
Правила: позначай зроблене ✔ (з одним рядком пояснення), тримай чергу актуальною, в кінці сесії дописуй підсумок.

## Правила перевірки

Після КОЖНОЇ зміни: `npm run test` + `npm run lint` + `npm run build` — усі зелені.
Кожне завершене покращення = малий коміт + `git push` в `origin/main` (remote не змінювати).
Зміна балансу XP має тримати `tests/economy.test.ts` у межах (normal 660–800 днів, discipline 320–460).

## Стан на старт

- [x] База v2.3 «прогресія до 100»: XP-крива до 100, анлоки вправ за рівнем, капове навантаження.
- [x] Виправлено: бонус +40 за повний день не фармиться між переходами; undo повертає XP з множником дня; rollover застосовує штраф за кожен пропущений день; старт для новачків — легкі варіанти (віджимання від колін, присідання з опорою) до 3 рівня.
- [x] Тести: 43 passed / 1 skipped. Lint чистий. Build OK.

## Що далі (черга-ідей)

- [x] Супервайзер `скрипт/run-improve.ps1`: запускати саме `node_modules\opencode-ai\bin\opencode.exe` (через `Get-Command opencode.cmd/.ps1/opencode` + фолбек на сам шлях) — `Start-Process` на `.cmd`-шим ненадійний. ✔ закомічено і запушено.
- [x] Множник XP винесено в спільну функцію `xpMultiplier(streak, habit)` / `xpMultiplierParts` (`src/game/leveling.ts`); прибрано дублікати формули з GameContext, Dashboard і economy-тесту. ✔ Відображення: дашборд показує множник навіть при серії 0; сторінка квестів показує «Множник XP ×… (серія ×… · звичка ×…)» під навантаженням дня. Перевірено у браузері: gain відповідає множнику.
- [x] Покриття тестами rollover з «хворим днем» за межами одного дня та багатоденні прогули з `sickUsed` біля ліміту 30 днів. ✔ 6 нових кейсів: прогалина 3 дні з «хворим» посередині (−10/−5/−10), два хворих поспіль (серія заморожена, рекорд не змінюється), хворий+прогул, ліміт токенів 3 у вікні, випадіння старих токенів із вікна, спожиті токени згоряють у rollover.
- [x] Доступність/UX: всі дії — нативні `<button>`/`<input>` (клавіатура працює). ✔ Додано `aria-pressed` на всі toggle-кнопки (інтенсивність, чек-ін болю, «хворий день», звук); лейбли форми в Settings зв'язані з інпутами через `htmlFor`/`id`.
- [x] Скористатися backlog з `NOTES.md` («Ідеї на майбутнє») — брати швидкі та самодостатні ідеї.
- [x] Побажання/ідеї на наступні сесії: (1) гейм-тест із miss-патернами — скористатися «хворим днем» на поточному реальному стані (брузерний профіль уже досяг рівня 4, streak 1, habit 5, totalXp 412); (2) продумати захист від «нечесного перемотування дати» (зараз гравець може відкатити годинник пристрою / змінити localStorage вручну); (3) перевірити словник UA на природність нових формулювань. ✔ Частково виконано: словник виправлено (iteration-4), miss+хворий-день перевірено в браузері (iteration-4/5, тости з'являються).
- [ ] **Штраф за прогул** — м'яким, але помітним (backlog NOTES.md). ✔ Частково у iteration-5: тост «День пропущено»; банер-бос лишається як велика ідея.
- [ ] **PWA** — встановлюється на телефон, офлайн. (Середнє за обсягом; потребує manifest + service worker — на окрему сесію.)
- [x] Захист імпорту від кривого JSON (зовнішній рівень `normalizeState`). ✔ Коміт `6fa489e`: не-масив `questsByDate`/зламані квести більше не ламають застосунок, стати/звичка/серія чистять за типами; +6 тестів.
- [x] Тести `levelInfo`/`classNameFor`/`powerScore`. ✔ Коміт `6075f56`: границі кривої XP, цикл класів/епітетів, powerScore.
- [x] Гейм-тест фактичного балансу: пройти 1–3 рівні в real браузері, звірити відчуття темпу з economy-симуляцією. ✔ Пройдено 3 повні дні по 5 квестів + 1 день-артефакт (miss через ручний зсув дати). Темп: день 1 → рівень 2 (144 XP), день 2 → рівень 3 (266 XP), день 3 → рівень 4 (412 XP, boost-день ×0.80). Відповідає «легкому старту»; крива рівнів 1–12 (70→570 XP на рівень) збігається з UI-показниками (перевірено `levelInfo`).

## Підсумок сесії

- Сесія 2026-09-23 (FINAL ITERATION): дописано покращення супервайзера — резолюція `opencode` для `Start-Process` (bundled `opencode.exe`). Коміт `a94302a`, запушено в `origin/main`.
- Сесія 2026-09-23 (v2.4-хит): множник XP — спільна функція `xpMultiplier`/`xpMultiplierParts` у `leveling.ts`, видалено дублікати формули (GameContext, Dashboard, economy-тест); відображення множника на сторінці квестів + на дашборді при серії 0. Real-browser перевірка: gain 9×0.8=7 збігається з показником. Тести: 46 passed / 1 skipped, lint чистий, build OK.
- Сесія 2026-09-23 (iteration-3): (1) баг — `SET_INTENSITY` ігнорував `soreGroups` при регенерації дня, позначені «болячі» групи зникали після зміни інтенсивності (коміт `be3d673`, +тести редуктора в `tests/state.test.ts`: new export `reducer`); (2) копія-фікс у Quests: «заміна без втрати XP» — брехня, бо XP залежить від нової вправи, текст переписано (коміт `7af0047`); (3) гейм-тест балансу в browser: 3 повні дні → рівень 4, sore-groups regression перевірено живцем.
- Сесія 2026-09-23 (iteration-4): (1) тост для «хворого дня» — rollover кидав event `sickDay`, але EventLayer його не рендерив; тепер гравець бачить «Хворий день зараховано: серія збережена, звичка −5» (коміт `972473c`, перевірено в браузері: habit 20→15, streak 3 збережено); (2) словник UA: прибрано неграматичне «Звірись:», уточнено, що +5 вранці дає саме основний квест (коміт `3ad7da5`); (3) в тестах підтверджено, що контракт події `sickDay` давно покритий (storage.test.ts).
- Сесія 2026-09-23 (iteration-5): (1) тост для повного прогулу — подія `miss` (коміт `669b065`): «День пропущено: серія згоріла, звичка −10»; перевірено в браузері (streak 4→0, habit 8→−2→0), +тест контракту; (2) баг — `SET_INTENSITY` регенерував день, але НЕ скидав `swapsUsed` (на відміну від `SET_SORE_GROUPS`) — користувач витрачав заміни на старий набір (коміт `285f42b`, +тест редуктора).
- Сесія 2026-09-23 (iteration-6): (1) тести рівневої логіки `levelInfo`/`classNameFor`/`powerScore` (коміт `6075f56`) — покрито границі кривої (0→70→190 XP), цикл класів і епітетів кожні 30 рівнів, powerScore = сума статів + habit/10; перевірка виявила, що епітет «Крижаний» з'являється на 121, а не 91 (виправлено в тесті згідно з константами); (2) санітайзер імпорту (коміт `6fa489e`) — `normalizeState` раніше міг прийняти JSON, який ламає застосунок: не-масив у `questsByDate` (падіння `.some`/`.map`), рядки/NaN у статах (NaN-полюція), сміття у `currentDate`/профілі. Тепер: totalXp лише скінченний ≥0, дні зі зламаними квестами відкидаються, стати/profайл/настройки/звичка/серія/токени чистять за типами, `dayIntensity` лише light/normal/intense. +6 тестів на ворожий вхід.
(3) консистентність вікон на дашборді — `lastNDays(14/7)` у Dashboard рахувався від справжньої дати пристрою, тоді як Progress-графік із `state.currentDate` (коміт `ba18eda`); тепер обидва показують один і той самий ігровий час.
- Сесія 2026-09-23 (iteration-7): (1) тести-гарди `SWAP_QUEST` — виконаний квест і вичерпаний денний ліміт замін не дають заміну (коміт `8f407bd`; з'ясувалось, що `SWAPS_PER_DAY` = 3, а не 2); (2) **журнал ваги** (коміт `267647b`) — давній запит мотивації з backlog: STATE_VERSION 5 + `weightHistory` (міграція v4→v5, кап 520 записів, один запис на день), reducer `ADD_WEIGHT` (заміна за ту саму дату, валідація 0–500 кг), панель «Вага тіла» в Налаштуваннях (інпут + список останніх 6 із дельтою, Enter/кнопка «Записати»), санітайзер імпорту для `weightHistory`; перевірено в браузері: міграція старого сейву v4→v5, запис 78.5 кг з'явився в localStorage і в списку.
- Сесія 2026-09-23 (iteration-8): (1) гейм-тест рівнів 5–8 у браузері — все зійшлось: рівень 5 = 2 підходи, бадж розігріву ×0.9 (0.97 загалом), методична XP (78 = 80×0.97), рівень 8 «Лицар» = 3 підходи, повне навантаження ×1.08 (intense×0.8, розігрів 1.0, XP 54 ≈ 50×1.08), важкі вписи типу dips у пулі. Багів не знайдено; (2) вага на дашборді (коміт `8f0a412`) — панель «Динаміка» тепер показує KPI «вага · кг» (останній запис) + нотатку «−0.8 до минулого»; панель мотивує щотижневий запис із Settings.
- Сесія 2026-09-23 (iteration-9): (1) **вибір варіанта вправи зважено** (коміт `7604cc7`) — раніше форма обиралась рівномірно серед розблокованих: на рівні 8 могла випасти найлегша «з опорою». Тепер вага = minLevel варіанта → складніші форми домінують із ростом рівня (на 18: сумо/вистрибування ≈ 75% проти стартових), детермінізм дня збережено. +3 тести (l1 на рівні 1, домінування топ-форм через 800 днів на 18 і 8). Під час налагодження з'ясувалось: присідання випадають лише ~12–14% днів (pickStrength-бакети), сценарії тестів враховано; (2) **мінімальний PWA** (коміт `9a7a0d1`) — manifest.webmanifest (темна тема, standalone, SVG-іконка з favicon), service worker sw.js (керває app-shell + асети, офлайн-фолбек для навігації), реєстрація лише в PROD, мета theme-color. dist містить sw.js і manifest; (3) **живий таб-тайтл** (коміт `52b8da9`) — заголовок вкладки показує клас і рівень («FitQuest — Лицар · 8»), міні-мотиватор без вкладки.
- Сесія 2026-09-23 (iteration-10): **екран-привітання «Як грати» для новачка** (коміт `b92765e`) — STATE_VERSION 6 + прапорець `onboardingDone` (createInitialState false, міграція v5→v6, санітайзер булевим), reducer `COMPLETE_ONBOARDING`, модал в App.tsx (role=dialog, aria-modal): 3 підказки про дисципліну/легкий старт/чек-ін болю + CTA «До квестів» (одразу веде на сторінку квестів). Перевірено в браузері: новий сейв → модал → клік → сторінка квестів, onboardingDone=true та version=6 у localStorage, після перезавантаження модал не з'являється. Тести: міграції v1→v6...v6 без змін, редуктор.
- Сесія 2026-09-23 (iteration-11): (1) **збереження активної сторінки** (коміт `2334475`) — після reload під час тренування повертаєшся на «Квести», а не на «Персонаж»: поточний розділ у sessionStorage (ключ `fitquest-page`), валідація ключа, fallback на dashboard. Перевірено в браузері: клік на «Прогрес» → reload → залишаємось на «Прогрес»; (2) **apple-touch-icon** (коміт `7c48d3f`) — PNG 180×180 згенерований канавою браузера за геометрією favicon.svg (#0a0d13 + штрихи #ecc878 + крапка #ffd98a), лінк в index.html, копіюється в dist; Safari тепер візьме іконку для Home Screen.
- Сесія 2026-09-23 (iteration-12): (1) **guard-тест PWA** (коміт `c75f33b`) — `tests/pwa.test.ts` (5 тестів): manifest валідний JSON (standalone, start_url, SVG-іконки any+maskable), sw.js містить install/activate/fetch + navigate-фолбек, apple-touch-icon валідний PNG (магічні байти), index.html посилається на manifest/icon/theme-color/mobile-web-app-capable, main.tsx реєструє SW під `import.meta.env.PROD`. Рефакторинги більше не «загублять» офлайн-функціонал мовчки; (2) **панель «Бос тижня»** (коміт `0330ed3`) — на «Прогресі» рядок із 7 клітинок останніх днів (золоті = повністю виконані, теплі = часткові), лічильник «Переможних днів: X/7», при 7/7 — «Бос переможений», іконка Crown (додана в Glyphs). Read-only, без стану, без економіки.
- Сесія 2026-09-23 (iteration-13): **таймер «Перерва 5 хв»** (коміт `fc15e86`) — панель на «Квестах» між списком і чек-іном: відлік 05:00, старт/пауза/скидання, при завершенні — звук `playTimerDone()` (нова дрібниця у sound.ts) + зелений рядок «Перерву завершено — час рухатись!», повага до settings.sound, очищення інтервалу при розмонтуванні. Перевірено в браузері: панель рендериться, після Старту через 2.5с показує 04:58. Backlog NOTES.md оновлено: PWA ✔, таймер ✔ (частково — без Notification API), бос тижня частково (без нагороди).
- Сесія 2026-09-23 (iteration-14): **досягнення «Переможець тижня»** (коміт `ab44b3b`) — `boss_week` у ACHIEVEMENTS: 7 останніх днів (через `lastNDays(7, currentDate)`) повністю виконані → трофей + raw event; момент перевірки саме при завершенні квесту — на останньому квесті 7-го дня подія спрацьовує одразу, тож rollover-перевірка не потрібна (підтверджено тестами редуктора). Іконка Crown в ACHIEVEMENT_ICONS. Нові тести `tests/achievements.test.ts` (6): чиста перевірка + reducer-інтеграція (значення незаблокованого = дата розблокування, не `true`). Backlog NOTES.md: бос тижня тепер повністю реалізований (панель + нагорода).
- Сесія 2026-09-23 (iteration-15): **a11y: тости оголошуються скрінрідером** — контейнер `.toast-area` отримав `role="status"` + `aria-live="polite"`: досягнення, «День завершено», нагадування про пропуск озвучуються, але не перебивають. Черга після цього: Notification API (для фонових нагадувань поза вкладкою) та «майже-досягнення» (прогрес 6/7 до боса).
- Сесія 2026-09-23 (iteration-16): **сортування трофеїв** — на «Трофеях» відкриті досягнення тепер перші, всередині — найновіші спершу (за датою розблокування), закриті — у визначеному порядку. Щойно отриманий трофей одразу видно без скролу.
- Сесія 2026-09-24 (iteration-17): (1) **Браузерні сповіщення для таймера перерви** (коміт `b212193`) — `STATE_VERSION` 7, налаштування `notifications`, запит дозволу, відправка сповіщення при завершенні таймера перерви; (2) **Прогрес досягнень на заблокованих трофеях** (коміт `d683edf`) — функція `getAchievementProgress` у `achievements.ts`, бейдж прогресу на заблокованих картках на сторінці «Трофеї», тести в `achievements.test.ts`.
- Сесія 2026-09-24 (Autonomous Iteration 2): **банер підтримки при пропущеному дні** (коміт `145d4e2`) — на сторінці «Квести» додано м'який попереджувальний банер із закликом до дії, якщо попередній день було пропущено (не виконано основних квестів і не був хворим днем) і досі немає прогресу за сьогодні. З можливістю закрити («Зрозуміло»). Тести (`npm run test`), лінт (`npm run lint`) та білд (`npm run build`) успішні, зміни запушено в `origin/main`.
- Сесія 2026-09-24 (Autonomous Iteration 3): **компаньйон «Фіт-Бот Спаркі»** — інтерактивний супутник на Дашборді (`src/game/companion.ts` + `tests/companion.test.ts` + панель у Dashboard.tsx), який реагує на серію, звичку та статус квестів дня (настрій від дрімоти до тріумфу + цитати-мотиватори). Успішно пройдено тести (107 passed), лінт та прод-білд, зміни зафіксовано та відправлено в `origin/main`.
- Сесія 2026-09-24 (Autonomous Iteration 4): **експорт/копіювання звіту прогресу** — нова панель «Поділитися прогресом» у Налаштуваннях (`src/pages/Settings.tsx`): кнопка «Скопіювати звіт» формує зручний текстовий підсумок персонажа (імя, клас, рівень, серія, звичка, XP, сила, квести) з копіюванням у буфер обміну та підтвердженням. Успішно пройдено тести (107 passed), лінт та прод-білд.
- Сесія 2026-09-24 (Autonomous Iteration 5): **святкування завершення дня** — при виконанні всіх квестів дня (`dayComplete`) тепер спрацьовує святковий звуковий ефект досягнення (`playAchievement()`) та анімація конфеті (`<Confetti />`), що робить закриття дня більш яскравим і нагороджуючим. Успішно пройдено всі тести (107 passed), лінт та прод-білд, зміни зафіксовано та відправлено в `origin/main`.
- Стан перевірок: `npm run test` — 107 passed / 1 skipped, `npm run lint` — чисто, `npm run build` — OK.
- Сесія 2026-09-24 (FINAL ITERATION): фінальна перевірка та завершення сесії. Усі тести (107 passed), лінт та білд зелені. Обновлено журнал `AUTO_IMPROVE.md`, фінальний коміт та push виконано.
- Черга на наступну сесію: сесію успішно завершено.