# NOTES — пам'ятка проєкту FitQuest

Цей файл — робоча пам'ятка. Сюди записую поточний статус, що зроблено і що хочу додати надалі.
Дата останнього оновлення: 22.09.2026

## Що зроблено (v1)

- [x] Vite + React + TS, темна фентезі-тема (золото/чорнило)
- [x] Система XP і рівнів, класи персонажа
- [x] Характеристики + система «Дисципліна»
- [x] Генерація 5 квестів/день з прогресивним навантаженням
- [x] Сторінки: Персонаж / Квести / Прогрес / Трофеї / Налаштування
- [x] Досягнення, конфеті + звук на level-up
- [x] Серія днів (streak), рекорд серії, бонус до XP
- [x] Розрахунок дня при «перекресленні» дати (rollover)
- [x] localStorage + експорт/імпорт JSON + повне скидання
- [x] Іконки замість емодзі (lucide-react): `src/components/iconMaps.ts`, хелпери у `src/components/Glyphs.tsx`.
- [x] GAME_DESIGN.md з балансом

## Що зроблено (v2 — «нескінченна гра»)

- [x] **Сегментна крива XP**: розгін (1–12: 70+50/lvl) → круїз (13–30: 550 const) → підйом (31–60: +10/lvl) → марафон (61–80: +13/lvl) → фінал (81–100: +45/lvl). Сума ≈84k XP; скасовано ранню ідею «650 const назавжди». Епітети-цикли для класів з 31+ рівня.
- [x] **Дисципліна = звичка** (0–100): старт 0, +5/завершений день, −10/прогул; формування за ~20 днів. Стат `discipline` видалено (3 стати). `bestHabit` для трофея.
- [x] **Градація навантаження**: цикл днів тижня (Нд 0.6 … Вт/Пт 1.2) × ручна інтенсивність (0.7/1.0/1.35), сумарно clamp [0.5, 1.6]. Неділя — день відновлення (3 квести).
- [x] **Варіанти вправ**: `variantId` + опис; на високих рівнях кап повторів, а форма вправи змінюється.
- [x] Множник XP виправлено на коректне множення: `(1 + min(streak×0.02, 0.2)) × (0.8 + 0.2×habit/100)`.
- [x] Досягнення розширено до 20 (серії 66/100, рівні 30/50/100, квести 1000, звичка 100).
- [x] Перерви/розтяжка більше не дають характеристику (стат опційний).
- [x] Сховище: новий ключ `fitquest-state-v2`, STATE_VERSION 2, авто-міграція з v1.
- [x] Селектор інтенсивності дня (закривається після першого виконаного квеста) + бейдж типу дня/навантаження на сторінці квестів.
- [x] Build + lint чисті; функціонально перевірено real-browser (виконання, undo, інтенсивність, rollover для завершеного дня та прогулу, персистентність).

## Що зроблено (v2.1 — «м'який старт + мотивація»)

- [x] **Warm-up розігрів на низьких рівнях**: коефіцієнт навантаження 0.75 (рівні 1–3) → 0.9 (4–6) → 1.0 (7+). Застосовується і до репсів, і до XP; бейдж дня показує підсумковий множник (день × інтенсивність × розігрів).
- [x] **Прогресивні підходи**: `setsFor` — 1 підхід на рівнях 1–2, 2 підходи на 3–5, 3 підходи на 6+. Об'єм зростає з рівнем, старт м'який (день 1: 1×5 віджимань).
- [x] **Чергування груп м'язів**: вправи мають `muscle` (push/leg/core), 2 силові квести дня беруться з різних груп; квест не повторює вчорашню силову з тієї ж групи.
- [x] **Розумна неділя**: основний квест — тільки з `rest:true` (прогулянка/планка/супермен/бічна планка) + 2 легкі перерви.
- [x] **Токен «хворого дня»**: до 3 використань за ковзні 30 днів. Позначив день → серія заморожується (не згорає, не росте), звичка −5 замість −10; якщо все ж потренувався — токен повертається. Кнопка-перемикач на сторінці квестів.
- [x] **Історія звички** (`habitHistory`, кап 90 точок): панель «Динаміка» на дашборді — тренд звички за 7 днів, «≈N днів до 100», ідеальні дні X/14, квести за 7 днів.
- [x] **Підказка при спаді**: якщо звичка < 30, сьогодні ще не тренувався та вже ≥18:00 — м'який натяк почати з перерви.
- [x] Сховище: STATE_VERSION 3 (міграція v2→v3 додає `sickUsed`, `habitHistory`), ключ лишається `fitquest-state-v2`.
- [x] Build + lint чисті.

## Що зроблено (v2.2 — «система самодогляду»)

- [x] **Чек-ін стану тіла**: чіпи болючих груп (Руки/плечі, Ноги, Спина/кор) до першого виконаного квеста. Позначені групи виключаються з силових; 1 група → день легший (4 квести), 0 груп → відновлення (3 квести). Перегенерація скидає лічильник замін.
- [x] **Заміна вправи** (`↻`, до 3/день, `SWAPS_PER_DAY`): основний лишається основним; пріоритет — та сама група → та сама категорія → легка → будь-яка основна; без дублів із призначеними. Seed `«дата-шаблон-№заміни»`.
- [x] **Частковий кредит дня**: лише не-основні виконані → `HABIT_PARTIAL_GAIN` +2, серія заморожується, подія/тост `dayPartial`.
- [x] **Кул-даун груп** (день-2): силові наступного дня віддають перевагу групам, яких не було вчора; множина «вчорашніх» — реально виконані вправи з `questsByDate` (rollover, зміна інтенсивності, чек-ін).
- [x] Додано силову для **core** (`crunch`) — тепер три повні бакети push/leg/core, ротація реально працює.
- [x] Сховище: STATE_VERSION 4 (міграції v1/v2/v3→v4 додають `swapsUsed: 0`, `soreGroups: []`), ключ лишається `fitquest-state-v2`; rollover скидає заміни/біль.
- [x] **Автотести (vitest)**: `tests/quests.test.ts` (warm/sets/eff, генерація, кул-даун, sore, swapFor, детермінізм) + `tests/storage.test.ts` (rollover full/partial/sick/refund/miss, історія+кап, міграції). `npm run test` — 30 тестів зелених.
- [x] Build + lint чисті.

## Що зроблено (v2.3 — «прогресія до 100»)

- [x] **Об'єм до плато**: формула `min(cap, base + (lvl-1)×grow)` — більше ніяких «рваних» підйомів (поле `step` → `grow`), кожна сім'я має власний кап (планка 120с, віджимання 60, берпі 30, …).
- [x] **Рівневі анлоки**: `minLevel` на сім'ях; експорт `unlockedTemplates(level)` обмежує генератор/заміни; `unlocksBetween(a,b)` → подія `unlock` на level-up + модалка «Відкрито нові вправи». Нові вправи без обладнання: скручування по-російськи (10), стрибок-групування (12), піка (14), пістолет (18), порожній-холд (20), стійка на руках біля стіни (24), ведмежа хода (26), лучник (45), віджимання на 1 руці (70; SE-варіант), розминки зап'ясть/очі/ікри/біч.
- [x] **Панель «Попереду на шляху»** на дашборді: наступні 3 анлоки + найближчий клас.
- [x] **Економіка-симуляція** `tests/economy.test.ts` (детермінований seed): профілі «звичайний» (0.65 хвилі основних / 0.25 один / 0.1 пропуск) і «дисципліна» (0.85 все/0.03/0.02). Підсумок калібрування: casual → 100 за **716 днів** (~2 роки), discipline → **359** (~1 рік). Границі в асертах: 660–800 і 320–460.
- [x] Тести quests розширено (об'єм: монотонність+плато, анлоки: гейтинг/не-в-замінах/unlocksBetween). `npm run test` — 41 зелений + 1 skip (калібрувальна таблиця, включається вручну).
- [x] Build + lint чисті; розділи GAME_DESIGN/NOTES оновлені.

## Відкриті питання / нюанси

- Баланс XP відкалібровано симуляцією (див. v2.3): «нормальні» 1–3 основні на день → ~118 XP/день → 100 рівень за ~2 роки; дисципліна ~233 XP/день → ~1 рік. Фактичний гейм-тест усе ще бажаний.
- Квести детерміновані тепер від дати **+ інтенсивності** (зміна інтенсивності міняє набір дня).
- Звуки: AudioContext створюється лениво при першій події.
- Множник XP на старті (звичка 0) = 0.8: навіть з серією до 0.96. Очікувано — звичку треба будувати.
- Rollover звички: +5 якщо вчора був завершений ≥1 основний квест; інакше −10. Якщо вчорашній день (дата згорілого квесту) був позначений як «хворий» і квестів не зроблено — серія заморожується, звичка −5, токен витрачається.
- Warm-up діє і на XP: на рівні 1–3 фактичний gain за день нижчий за номінальний (~×0.75–0.9), це враховано в балансі (м'який старт).
- Неділя — день відновлення як по набору квестів, так і по `rest:true` шаблонах основних вправ; «важкі» дні (Вт/Пт) при цьому лишаються силовими.
- Токен «хворого дня» рахується у ковзному 30-денному вікні від `currentDate`; позначити/скасувати можна в будь-який момент дня, доки не зроблено жодного квеста.

## Ідеї на майбутнє (backlog)

- **Бос тижня / щотижневий виклик**: панель із 7 днів на «Прогресі» готовa ✔ (iteration-12); лишилась нагорода: міні-досягнення через ACHIEVEMENTS + перевірка на rollover.
- **Таймер «Помодоро-перерви»**: панель «Перерва 5 хв» на «Квестах» — старт/пауза/скидання, звук по завершенню ✔ (iteration-13); можна додати нагадування «встати» у фоні (Notification API).
- **Компаньйон**: пітом, що «росте» від серії днів і «сумує», якщо пропускаєш. (Міні-графіка, без анімацій на старті.)
- **PWA**: встановлюється на телефон, офлайн ✔ (iteration-9: manifest + sw.js; iteration-11: apple-touch-icon; iteration-12: guard-тести).
- **Штраф за прогул** — зробити м'яким, але помітним: банер «Бос сумує» замість голого мінуса у дисципліні.
- ~~**Графік ваги** (введення ваги щотижня)~~ ✔ (v2.5 iteration-7): STATE_VERSION 5, поле `weightHistory` (один запис на день, кап 520), панель «Вага тіла» в Налаштуваннях (+ список останніх 6 із дельтою до попереднього).
- **Більше «перерв-квестів»**: скручування, розминка зап'ясть для розробника (готові: `crunch`, `wrist_stretch`), «Ходьба по сходах» ✔ (v2.4).
- **2 мови (UA/EN)**, якщо захочеться ділитися з іншими.
- **Dark/Light теми** — наразі тільки темна.

## Стек / команди

- `npm run dev` — розробка
- `npm run build` — прод-збірка (tsc + vite)
- `npm run lint` — oxlint
- `npm run test` — vitest (квести + сховище + економіка)
- Дані у localStorage, ключ `fitquest-state-v2`. Міграція при зміні схеми: поле `version` у state.